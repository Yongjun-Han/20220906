let operandOne = "test";
let operandTwo = 4;

let result = (operandOne.length === operandTwo) ? "hello" : "bye";

console.log(result);

//조건문 ? --> 조건문이 참일경우 콜론 앞의 값 거짓일경우 콜론 뒤의 값
//condition ? exprIfTrue : exprIfFalse 